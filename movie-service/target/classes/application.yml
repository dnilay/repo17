logging:
  file:
    name: movie-ws.log
management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: 3000
spring:
  datasource:
    password: sa
    url: jdbc:h2:mem:testDB
    username: sa
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
